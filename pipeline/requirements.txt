# Core
ultralytics>=8.3.0            # YOLO11 person detector
transformers>=4.57.0          # Qwen3-VL support (Qwen3VLForConditionalGeneration)
accelerate>=0.34.0
torch>=2.4.0                  # on Colab/HF this is preinstalled; don't reinstall there
Pillow>=10.0.0
huggingface_hub>=0.35.0
pandas>=2.0.0
pyarrow>=15.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Optional: 4-bit quantization (fit Qwen3.5-27B on a single 40GB A100)
#   pip install "bitsandbytes>=0.43.0"
# Optional: fastest attention (attn_implementation: flash_attention_2)
#   pip install flash-attn --no-build-isolation
# Optional high-throughput captioning backend (for the full 100k+ run).
# Install separately, it pulls a specific torch build:
#   pip install "vllm>=0.11.0"
